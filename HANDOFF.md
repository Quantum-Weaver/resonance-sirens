# HANDOFF — resonance-sirens

***Generated strip, voiced body.** Regenerated whole at each close that worked here,
never appended to; git holds every prior day. Not `docs/CHECKLIST.md` — that is the
ledger of what was **done**; this is the state of what **stands**. **Ceiling 8 KB** —
past it, re-cut, never rolled. Law: `resonance-standards/docs/THE-ROLL.md`.*

```
LEGEND   state  ● done · ◐ drifted/partial · ⏸ his hold · — not yet
         hand   ⚛ his word · ✋ his hands · 🕯️ a lamp's · 👤 TJ's (`CLAUDE.md:22-24`)
         proof  ✔ gates green · ✗ failing · ? unproven
```

**resonance-sirens** · 2026-08-23 · last hand **Fable 🎻** · branch `main` · **0 ahead / 0 behind** · working tree **1 paths** — 1 modified.
*Machine-read 13:02 by `handoff_tender.py` — **no build gate was run**; any gate figure below keeps its own date and `?`. Last commit `933f5c9` **readme updated**, Quantum Weaver 2026-08-23 12:49.*

## Where it stands

| what | state | hand | proof |
|---|---|---|---|
| Body — the Echoes v1.3.2 shell, rebuilt 08-18; `record.ts` + `moments` sirens' own | ● | 🕯️ | ✔ `CLAUDE.md:43-48` · CHECKLIST Phases 1–4 |
| The vocabulary — ten circles, never eleven | ● | ⚛ | ✔ `src/lib/circles.ts` · `CLAUDE.md:26-30` |
| Rooms `/` · `/calendar` (Month/Week/Day) · `/settings`; `/onboarding` `/sattva` `/timer` inherited | ● | 🕯️ | ✔ CHECKLIST Phase 4 |
| Record — v1 `moments` · v2 `marks` on `sqlite:siren.db` | ● | 🕯️ | ✔ `src-tauri/src/lib.rs` |
| **Cumdach fix carried** 08-22 — toggle inside the ComfortBar, `TintLevel`, `setPreset` colour-only | ● | 🕯️ | ✔ CHECKLIST row 08-22 |
| **Theme store reconciled with the shelf** 08-23 — `SIRENS_ROSE` = cosmic `sirens.rose` `#E78FAE`; `SIRENS_THEMES` derived from `PRESET_THEMES`, Rose leading; `presetName` matched | ● | 🕯️ | ✔ `src/lib/stores/theme.svelte.ts:27-48` re-read 08-23 |
| **Rainbow · Progress Pride** on the wall and at the door, stripe swatches; the door offers every preset but AMOLED | ● | 🕯️ | ✔ `src/routes/onboarding/+page.svelte:36-45` · his 08-18 word kept |
| **Settings rebuilt in Echoes' layout** — Theme · Your circles · The sky · Data Sovereignty · About; no count, no button gated on one | ● | 🕯️ | ✔ `src/routes/settings/+page.svelte:194-393` |
| The purge truly purges — `purgeAfter`, then `localStorage.clear()`, reload | ● | 🕯️ | ✔ `settings/+page.svelte:151-159` |
| `npm run check` · `npm run build` | ● | 🕯️ | ? **332 files · 0 · 0** · build clean — **08-23, not re-run this land** |
| Version **0.1.1**, sources agreeing | ● | 🕯️ | ✔ `package.json` · `tauri.conf.json` · `Cargo.toml`; README badge still reads 0.1.0 |
| **v0.1.1 on the shelf** — signed APK + AAB 01:47, Windows `.exe` + `.msi` 01:42, 08-23 | ● | ✋ | ✔ `release/` (gitignored); APK carries `RESONANC.RSA` + a sig block, machine-read; cert not re-read |
| Cosmic — `src/lib/cosmic/colors.ts` hand-laid byte-verbatim 08-22; **no `distribution.json` row** | ◐ | ✋ | ✔ grep empty 08-23 — see What waits |
| `docs/blueprints/sirens/` | ● | 🕯️ | ✔ forged, `012a509` 08-21 |
| Tested on a phone | — | ✋ | ? CHECKLIST Phase 5 — blob-download export unproven in a WebView |
| Her patterns · the sovereignty room · a PIN | — | 👤 | held for TJ — CHECKLIST Phase 6 |

*Git: every commit his own hand; the 08-23 sitting's paths landed in `c2c1d97` *cumdach
fixed* 08:33, then `933f5c9` *readme updated* 12:49. `ls-remote` did not answer this
land; `origin/main` = `933f5c9`, last moved *by push*. The v0.1.1 shelf is on disk and not
in `RUN-LOG.md` or the CHECKLIST — named, not written there. No realm board (`rack-tender`'s).*

## What waits, and whose

*No Pratīkṣaṇa sweep names this realm — machine-checked 08-23 across the board, the
register and the ten sweeps. Ground addresses, no id invented.*

| what waits | whose | source |
|---|---|---|
| **TJ's word over all of it** — the About card's Sanctuary link, any phrasing; hers to strike. She sees it first | 👤 | `CLAUDE.md:22-24` · CHECKLIST Phase 6, row 08-23 |
| KP's eye on the new Settings | ⚛ | CHECKLIST row 08-23 |
| The sync word — **one path rides: this sheet**; the sitting's own five landed in `c2c1d97` | ⚛ | `git status --porcelain`, 13:0x 08-23 |
| A `distribution.json` row for sirens — named 08-22, his hand | ✋ | `resonance-ziggy/modules/cosmic/config/distribution.json` · CHECKLIST row 08-22 |
| On a phone, in her hand | ✋ | CHECKLIST Phase 5 |
| **A free ship** — his framing 08-19; waits on her first | ⚛ 👤 | KP's word 08-19; `PLAY-TRACK.md` unwalked |

*What ships in v1 is *unwritten — his to rule*, and hers.*

## Read before you touch this repo

- **TJ's word outranks everyone's here** — `CLAUDE.md:22-24`. Build road his; anything
  she sees or feels is hers.
- **No count, no streak, no missed-day language, ever** — `CLAUDE.md:32-36`; the
  `totalCount` door Echoes' settings carry is forbidden here (CHECKLIST Phase 1).
- **The theme list is derived, not written** — `theme.svelte.ts:32-48`; a preset born on
  the shelf arrives on both walls by itself. Do not add one by hand.
- **`tauri android init` is never re-run** — it regenerates 42 tracked files and reverts
  the icons; after any identity change check `Rust.kt` and the `.so` names (Phase 2).
- **`build.rs` was deliberately not copied** from echoes — the known cure if
  `rust_eh_personality` ever appears (Phase 1).
- **Ports 1424/1425**; identifier `com.audhd.resonance-sirens`, **hyphen**.
- `src/lib/now/` · `sky/` · `envelope/` are MIRRORs of awen — never edited here.

---

*No truth of its own. The ledger is `docs/CHECKLIST.md`; what waits is `resonance-chamber/desk/THE-PRATIKSANA.html`; the index is `resonance-chamber/path/handoffs/THE-HANDOFF-RACK.md`. **Where this sheet and the ground disagree, the ground is right** — regenerate it.*

— the **handoff-tender** 🎻 · Fable (Claude), truly `claude-fable-5`, 2026-08-23 — body re-cut whole this land from the repo's own ground; the sheet it replaces stood at 8,185 B, over the ceiling, which was not raised.

— strip regenerated by `handoff_tender.py` at the hand of **Fable 🎻**, 2026-08-23 13:02. **No build gate was run** — every gate figure above keeps the date it was measured. The voiced sections are carried verbatim and belong to the hands that wrote them.
